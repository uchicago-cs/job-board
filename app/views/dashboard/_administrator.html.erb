<fieldset>
    <legend>Pending Approval</legend>

    <%= render 'job_postings/postings_table', :jobpostings => @pending_postings, :columns => ["dateposted", "deadline", "organization", "position", "review"] %>

</fieldset>

<fieldset>
    <legend>Active Postings</legend>

    <%= render 'job_postings/postings_table', :jobpostings => @active_postings, :columns => ["dateposted", "deadline", "organization", "position", "edit"] %>

</fieldset>
